# اجرای بسته Release 0

## پیش‌نیاز

- Windows
- Python 3.11 یا جدیدتر
- دسترسی اینترنت برای نصب وابستگی‌های Python

## اجرای عادی با View شخصی پیش‌فرض CRM

روی فایل زیر دوبار کلیک کنید:

```text
run-app.bat
```

یا در PowerShell از ریشه پروژه اجرا کنید:

```powershell
.\run-release-0.ps1
```

این حالت View شخصی «داشبورد مدیریت همه موارد باز دست هلپ دسک» را به‌صورت پیش‌فرض انتخاب می‌کند، Caseهای آن را با دسترسی Read-Only واکشی می‌کند و Prototype را در نشانی زیر باز می‌کند:

```text
http://127.0.0.1:8765/
```

برای توقف برنامه، در پنجره PowerShell کلید `Ctrl+C` را بزنید.

## اجرای برنامه با داده واقعی CRM

فایل `run-app.bat` همیشه ابتدا با حساب Windows فعلی به CRM متصل می‌شود و Snapshot واقعی را با دسترسی فقط‌خواندنی واکشی می‌کند. اگر اتصال CRM یا احراز هویت موفق نباشد، برنامه با داده فرضی جایگزین ادامه نمی‌دهد.

اجرای CRM فقط درخواست‌های GET ارسال می‌کند. هیچ Case، Note، Task یا مقاله‌ای در CRM ایجاد، ویرایش یا حذف نمی‌شود.

## اجرای مستقیم سناریوی کندی سیستم

برای بازکردن مستقیم صفحه سناریو، روی این فایل دوبار کلیک کنید:

```text
اجرای-سناریوی-کندی.bat
```

این فایل ابتدا سرور محلی را اجرا می‌کند و سپس این صفحه را باز می‌کند:

```text
http://127.0.0.1:8765/scenario-checklist.html
```

در صورت نیاز به اجرای سرور بدون بازشدن مرورگر:

```powershell
.\run-release-0.ps1 -NoBrowser
```

## محل داده داخلی

```text
data\local-prototype.sqlite3
```

این فایل محلی و در `.gitignore` قرار دارد و نباید به مخزن یا سرویس اشتراکی منتقل شود.
